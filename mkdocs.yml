site_name: مستندات سازمان
site_description: سایت مستندات نمونه با MkDocs Material
site_url: !ENV [SITE_URL, "http://127.0.0.1:8000/"]

theme:
  name: material
  language: fa
  font: false
  palette:
    - scheme: default
      primary: custom
      accent: custom
      toggle:
        icon: material/weather-night
        name: حالت تاریک
    - scheme: slate
      primary: custom
      accent: custom
      toggle:
        icon: material/weather-sunny
        name: حالت روشن
  features:
    - navigation.sections
    - navigation.expand
    - navigation.top
    - search.suggest
    - search.highlight
    - content.code.copy

plugins:
  - search
  - awesome-pages

hooks:
  - hooks/breakless_lists.py

markdown_extensions:
  - admonition
  - attr_list
  - tables
  - mdx_truly_sane_lists:
      nested_indent: 2
      truly_sane: true
  - pymdownx.details
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - toc:
      permalink: true

extra_css:
  - stylesheets/extra.css

extra_javascript:
  - javascripts/mermaid-rtl.js
